# Secrets — never commit real keys
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Environments
.venv/
venv/
env/

# Generated plot artifacts
progress_traces*.png

# Verifier score caches and result tables — created on demand at runtime
cache/
results/

# Sphinx build cache (only _build/html is committed)
docs/_build/doctrees/

# Run logs
logs/

# StateMachineMcp (local state)
.opencode/
# Code Relay (local state)
relay.json
RESUME.md
next_step.md

# Private verification docs (contain local paths / LAN IPs)
docs/VERIFICATION-PLAN.md
docs/ref/

# Private deployment context (local paths / LAN IPs)
AGENTS.md
